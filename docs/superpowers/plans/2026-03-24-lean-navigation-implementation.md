# dwizzyWEEB Lean Navigation Implementation

Date: 2026-03-24

## Steps

1. Create a shared navigation config for top-level groups and leaf items.
2. Rebuild desktop navbar around grouped dropdown navigation.
3. Rebuild mobile bottom nav to `Home / Search / Bookmark / Menu`.
4. Add a mobile side panel that renders the grouped navigation tree and auth/account actions.
5. Add `/collection` as the bookmark landing page using the existing local-only saved/history surfaces.
6. Verify with `npm run lint` and `npm run build`.
